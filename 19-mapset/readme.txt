1) TreeMap can store both keys and its values. It extends the AbstractMap class and implements the NavigableMap interface. 
   It has a guaranteed runtime of log(n) for containsKey, get, put, and remove.
   TreeSet can store only the keys. It extends the AbstractSet class and implements the NavigableSet interface. 
   It has a guaranteed runtime of log(n) for add, remove, and contains.
